# Быстрый старт с Docker

## Запуск за 2 минуты

```bash
# 1. Клонируйте репозиторий (если ещё не сделали)
git clone <repository-url>
cd prak-mmp-5-semestr-task3

# 2. Запустите Docker Compose
make up
# или без make:
docker-compose up -d

# 3. Откройте в браузере
# Frontend: http://localhost:3000
# Backend API: http://localhost:8000/docs
```

## Проверка работоспособности

```bash
# Проверить статус контейнеров
make ps

# Проверить логи
make logs

# Проверить здоровье сервисов
make health
```

Должны быть запущены 2 контейнера:
- ✅ `ensembles-backend` (порт 8000)
- ✅ `ensembles-frontend` (порт 3000)

## Полезные команды

```bash
# Остановить
make down

# Перезапустить
make restart

# Пересобрать
make rebuild

# Посмотреть все команды
make help
```

## Что внутри?

### Backend
- FastAPI приложение
- Собственные реализации Random Forest и Gradient Boosting
- REST API для обучения и предсказаний моделей
- Swagger документация на `/docs`

### Frontend
- React приложение
- Интерфейс для взаимодействия с ML моделями
- Визуализация результатов

## Troubleshooting

### Порты заняты?
Измените порты в `docker-compose.yml`:
```yaml
ports:
  - "8001:8000"  # вместо 8000
  - "3001:80"    # вместо 3000
```

### Контейнер не запускается?
```bash
# Смотрим логи конкретного сервиса
docker-compose logs backend
# или
docker-compose logs frontend

# Пересобираем без кэша
docker-compose build --no-cache
```

### Нужно зайти в контейнер?
```bash
make shell-backend
# или
make shell-frontend
```

## Следующие шаги

1. Откройте http://localhost:8000/docs для изучения API
2. Откройте http://localhost:3000 для работы с UI
3. Изучите [DOCKER.md](DOCKER.md) для детальной информации
4. Изучите [README.md](README.md) для понимания алгоритмов

## Остановка и очистка

```bash
# Просто остановить
make down

# Остановить и удалить volumes
make clean

# Полная очистка (включая образы)
make prune
```

Готово! 🎉
